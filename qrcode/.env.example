# Environment Variables for QR Restaurant Ordering System

# Base URL for your application (required for QR codes and email links)
NEXTAUTH_URL=https://your-domain.vercel.app
NEXTAUTH_SECRET=your-random-secret-key-here

# Stripe Configuration (for payment processing)
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Email Configuration (optional - for sending receipts)
# Using Gmail as example - configure for your email provider
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Database URL (if using external database)
# Example for PostgreSQL on Supabase, PlanetScale, etc.
# DATABASE_URL=postgresql://username:password@host:port/database

# Example for MongoDB
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/database